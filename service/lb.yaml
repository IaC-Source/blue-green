apiVersion: v1
kind: Service
metadata:
  labels:
    app: dashboard
  name: dashboard
spec:
  ports:
  - protocol: TCP
    port: 18080
    targetPort: 80
  selector:
    app: dashboard
  type: LoadBalancer
